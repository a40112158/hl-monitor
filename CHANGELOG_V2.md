# Signal Model V2 优化说明

## 这次解决的问题

旧版把短期异动、长期观察和正式长期候选混在同一批收益样本中，容易出现“同一个信号被重复统计”“只观察也被当成长期单”“短期分数与长期分数口径不一致”等情况。V2 将准入、存储、回测和展示全部拆开。

## 新的信号定义

- **短期强信号**：`alert_score` 达到 `alert_score_push`，并且当前记录不是正式长期候选。
- **正式长期候选**：必须同时满足 `candidate_gate=PASS`、`candidate_state=CANDIDATE`，并且行动建议明确为“可进入低杠杆长期观察”。
- `FORMING`、`BLOCK` 和“只观察”不再写入长期收益样本。
- 长期候选的连续轮次包含当前轮，不再少算一轮。

## 评分与风险修正

- 修复空头方向的加减分符号：有利证据会增强方向分，不利证据会削弱方向分，多空保持对称。
- 滚动窗口风险只使用最终选中的窗口；未成熟且未选中的 30 天窗口不再污染成熟的 15 天窗口。
- 数据缺口只有在选中窗口覆盖率不足时才阻断长期候选，覆盖率足够时降级为提示。
- 短期阈值与长期阈值可独立调整，不再牵一发而动全身。

## 收益统计

- V2 回测只统计 `model_version=2`，V1 历史保留但不混算。
- 每个事件先扣除往返交易成本，再计算胜率、达标率、均值和中位数。
- 数据库记录每个周期的实际评估时间，报告会给出评估延迟。
- 小额减仓即使不生成噪声事件，也会计入已实现损益；未平仓交易的收益会合并已实现与未实现部分。

## 数据与运行安全

- 数据库自动迁移新增 V2 字段，不删除原有 V1 记录。
- 信号事件默认保留 90 天，普通运行历史仍按原有策略压缩。
- 当 `runs` 表为空时，压缩脚本不再误删原始快照。
- Windows 控制台无法显示个别字符时使用替换输出，避免扫描因编码问题中断。
- Cloudflare 继续通过 `workflow_dispatch` 触发；未传输入时不发送 Telegram 启动测试消息。

## 首次上线后的预期现象

V2 刚启用时，V2 回测显示“暂无成熟样本”是正常现象。旧数据不会被强行套用新定义；待新事件达到 1h、4h、24h 等评估周期后，统计会逐步出现。

## 验证结果

- Python 核心文件编译通过。
- 61 项自动化测试全部通过。
- 使用原有压缩数据库副本完成迁移检查，SQLite `quick_check` 正常，V1 数据保留且 V2 字段可用。
