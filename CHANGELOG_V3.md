# 收益面板与生命周期 V3

本次升级继续使用 Signal Model V2，不重置新信号口径；V3 是收益展示和交易周期层的升级。

## 已利用的现有数据

- 原数据库有 13 条 V1 短期事件，1h/4h/24h 收益均已成熟。
- 按每条事件扣除 0.12% 往返成本后，历史参考结果为：
  - 1h：13 条，普通胜率 53.8%，平均净收益 +0.37%。
  - 4h：13 条，普通胜率 38.5%，平均净收益 +0.51%。
  - 24h：13 条，普通胜率 61.5%，平均净收益 +2.13%。
- 原长期表有 5 条记录，但 5/5 都与短期事件重叠，而且原动作均为“只观察”。V3 会展示这一事实，但不会把它们当成有效长期成绩。
- 旧数据标记为 V1 历史参考，不进入 V2 胜率、置信度或自动阈值优化。

## 收益面板

- 短期只展示 1h、4h、24h。
- 正式长期只展示 72h、7d、15d、30d。
- 多头与空头分开统计。
- 每个周期分别显示成熟、待评估和超时缺值样本。
- 输出普通胜率、达标率、平均净收益和中位净收益。
- 新增 `reports/signal_performance_dashboard_v3.txt`。
- 新增 `reports/details/signal_performance_dashboard_v3_latest.csv`。

## 完整信号周期

- 生命周期状态为 `active → grace → closed`。
- 记录当前方向收益、扫描采样 MFE、MAE 和最大跟踪期限。
- 短期生命周期默认最多 24 小时，长期默认最多 720 小时。
- 退出类型区分失效、反转和到期。
- 生命周期存在期间禁止为同一连续信号重复建立固定周期收益样本。
- 同一 GitHub run 重试保持幂等，不重复写生命周期观察记录。
- 连续轮次现在会把健康扫描中“该币缺席”的轮次视为断点，不再跨缺失轮误算连续。

## 数据库与 GitHub 限制

- 保留原有单文件保护：本地数据库目标上限 75MB，压缩包硬保护 85MB，低于 GitHub 100MB 单文件限制。
- 新增字段和报表体积很小，不改变 Cloudflare `workflow_dispatch` 触发方式。
- 数据库迁移只增加字段和索引，不删除 V1 历史事件。
- GitHub Actions 只回写数据库、报告和自动阈值，不再暂存静态代码或手工配置，避免旧任务覆盖新部署。

## 验证

- 原数据库副本迁移后 `PRAGMA quick_check=ok`。
- V1 短期 13 条、原长期 5 条均被正确识别。
- 自动化测试覆盖收益周期分流、多空拆分、V1/V2 隔离、MFE/MAE、到期退出、幂等和连续轮断点。
